# Eval fixtures

Scenario JSON files live in the Python package:

`src/incident_commander/eval/fixtures/`

They are bundled with `pip install incident-commander` and used by default by `incident-commander eval`.
